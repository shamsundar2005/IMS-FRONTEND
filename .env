# Inventory backend (port 8080, no context-path)
VITE_API_BASE_URL=http://localhost:8080

# Material service — proxied through Vite dev server (see vite.config.js)
VITE_MATERIAL_API_URL=/api/material

# Vendor service — proxied through Vite dev server (see vite.config.js)
VITE_VENDOR_API_URL=/api/vendor
